#include<stdio.h>
#include<conio.h>
int main(){
    int a=15;
    if(a%2==0)
    {
        printf("the number is even",a);
    }
    else{
            printf("number is odd",a );
        }    
     (a % 2 == 0) ? printf("%d is even.", a) : printf("%d is odd.", a);
return 0;

}